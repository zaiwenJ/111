**【任务目标】** 请参考附件/上下文中的原型代码，将当前项目的“职称评审差距分析页面”进行彻底重构。本次重构需严格遵循以下核心设计方案与交互机制：

**一、 全局空间布局 (3:7 非对称分栏架构)**

1. **物理空间切分**：废弃原有的悬浮遮挡布局，采用左右 `3:7` 比例的平铺分栏（左侧最大宽度限制在 400px 内，右侧占满剩余空间）。
2. **独立滚动域**：系统高度锁定为 `h-screen`，绝对禁止 `body` 级别的全局滚动。左侧“源成果池”与右侧“申报矩阵工作台”必须配置独立的纵向滚动条 (`overflow-y-auto`)，确保跨区拖拽时视线不丢失。

**二、 矩阵导航与微型成果卡片 (UI 降噪设计)**

1. **区块级吸顶 (Sticky Header)**：右侧 A1/A2/A3 矩阵的标题栏必须采用 `position: sticky; top: 0` 设计，配合高级毛玻璃模糊 (`backdrop-blur-md`)。用户向下浏览内部卡片时，当前矩阵的准入规则和容量限制需始终吸附在顶部。
2. **高密度微型卡片**：严格收敛卡片信息，仅常驻展示四个核心元数据：名称（单行截断）、级别（彩色 Tag）、类别、顺位。赋予卡片 `hover` 态阴影加深和 `cursor-grab` 指针，强化物理抓取暗示。

**三、 拖拽前置防呆引擎 (Drag & Drop 状态机)**

- **瞬时视觉引导与阻断**：在 `onDragStart` 事件触发的瞬间（通过比对卡片类型与矩阵准入规则）：
  - **合规路径**：目标矩阵背景立刻泛起浅绿色 (`bg-emerald-50`) 并带有虚线边框，形成强引导。
  - **互斥/满载路径**：目标矩阵立刻叠加一层半透明灰色遮罩，并在中央显示红色的禁止图标（物理阻断），从源头切断非法投放操作。

**四、 底部智能控制台 (Smart Dock)**

- **彻底废弃游离悬浮球**，改用吸附在右侧工作区底部的可伸缩状态栏（Smart Console）。

1. **静默折叠态 (Slim Status Bar)**：高度约 40-50px，仅显示当前全局达标进度条和简要状态指示灯，不遮挡任何上方矩阵内容。
2. **事件驱动展开 (Auto-Expansion)**：当监听到 `onDrop`（卡片放置成功）事件时，底座**自动向上平滑展开**为控制台面板。
3. **双区面板结构**：
   - 左侧：极速展示底层 CSP 引擎算出的“硬性指标缺口”（如：缺1篇SCI，缺36课时）。
   - 右侧：提供“唤醒 AI 深度分析”的触发按钮。
4. **AI 流式推演链路**：点击 AI 按钮后，下方展开诊断区域。先展示骨架屏 (Skeleton) 过渡，随后以打字机流式效果输出具体的破局建议与跳转操作（CTA按钮）。

**五、 视觉质感与动效标准 (SaaS-Level UI)**

- 全局采用极简 SaaS 风格，大面积留白/灰底，仅在状态反馈（绿/红/橙）使用语义色彩。
- 底部 Smart Dock 的展开与折叠必须使用顺滑的 CSS 过渡动画（`max-height` 和 `opacity` 结合 `cubic-bezier` 曲线），绝不能出现生硬跳变。
- 在实现以上逻辑时，请将结构、样式与拖拽/底座伸缩的 JS 状态机逻辑合理拆分，以符合当前项目的工程规范。